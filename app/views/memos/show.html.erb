<p id="notice"><%= notice %></p>

<p>
  <b>Username:</b>
  <%= @memo.username %>
</p>

<p>
  <b>Subject:</b>
  <%= @memo.subject %>
</p>

<p>
  <b>Content:</b>
  <br />
  <% html = replace_uri((h @memo.content).gsub(/\r\n|\r|\n/, "<br />")) %>
  <%# safe_concat Sanitize.clean(html , :elements => ['a', 'br'], :attributes => {'a' => ['href', 'title', 'target'], 'br' => []})%>
  <%#= replace_uri(@memo.content.gsub(/\r\n|\r|\n/, "<br />")) %>
  <%= html.html_safe %>
</p>


<%= link_to 'Edit', edit_memo_path(@memo) %> |
<%= link_to 'Back', memos_path %>
